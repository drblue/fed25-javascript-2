# (very smol) Övning 2026-08-27

Skriv nedan tester för `<TodoCounter />`. Lägg testerna i `src/assets/todos/__test__/TodoCounter.test.tsx`.

Testa inte bara att siffran är rätt utan även att pluraliseringen stämmer.

```tsx
it("Shows correct count with no todos", () => {
  // Render
  render(<TodoCounter count={0} />);

  // Find

  // Assert
});

it("Shows correct count with a single todo", () => {
  // Render
  render(<TodoCounter count={1} />);

  // Find

  // Assert
});

it("Shows correct count with multiple todos", () => {
  // Render
  render(<TodoCounter count={5} />);

  // Find

  // Assert
});
```
