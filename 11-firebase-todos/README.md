# Firebase Todos

## Installation

Create a new Firebase project. Add a Web app to the project. Copy `.env.example` to `.env` and fill in the configuration from the Firebase app.

```zsh
npm install
```

## Run

```zsh
npm run dev
```

## Deploy

```zsh
npm run check
npm run build
```
